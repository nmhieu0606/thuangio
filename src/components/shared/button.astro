---
export interface Props {
  title: string;
  bg_color: string;
  hover_color?: string;
  rounded: string;
  text_size: string;
  dark_bg_color: string;
  other?: string;
  text_color:string,
  text_color_dark:string,
  href:string,
}
const {title,bg_color,hover_color,rounded,text_size,dark_bg_color,other,text_color,text_color_dark,href}=Astro.props;
---

<button class={`bg-${bg_color} hover:bg-blue-700 text-white font-bold py-2 px-4 rounded`}>
  Button
</button>

